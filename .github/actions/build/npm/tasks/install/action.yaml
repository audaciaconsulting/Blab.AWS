name: 'NPM Install'
description: ''

inputs:
  path:
    required: true
    type: string

runs:
  using: "composite"
  steps:
    # - name: Change directory to project path
    #   shell: bash
    #   run: cd ${{ inputs.path }}
    - name: Install packages
      shell: bash
      run: npm install --prefix ${{ inputs.path }} --prefer-offline --no-audit