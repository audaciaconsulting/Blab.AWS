name: 'NPM Build'
description: ''

inputs:
  path:
    required: true
    type: string

runs:
  using: "composite"
  steps:
    - name: Authenticate with the npm feeds
      shell: bash
      run: npx vsts-npm-auth -config ${{ inputs.path }}/.npmrc