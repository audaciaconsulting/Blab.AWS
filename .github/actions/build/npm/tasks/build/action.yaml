name: 'NPM Build'
description: ''

inputs:
  path:
    required: true
    type: string

runs:
  using: "composite"
  steps:
    - name: Change directory to project path
      shell: bash
      run: cd ${{ inputs.path }}
    - name: Build project
      shell: bash
      run: npm run build