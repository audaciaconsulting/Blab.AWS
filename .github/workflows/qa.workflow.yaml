on:
  push:
    branches: [ "dev" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: ./.github/workflows/stages/build.stage.yaml
  deploy-qa:
    runs-on: ubuntu-latest
    steps:
    - uses: ./.github/workflows/stages/deploy.stage.yaml
      with:
        environment-name: 'Quality Assurance'
        environment-id: 'qa'