export function maximumBlabLength(text: string, maxLength: number): boolean {
  return text.length > maxLength;
}

export function minimumBlabLength(text: string, minLength: number): boolean {
  return text.length < minLength;
}
